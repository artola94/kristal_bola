# Kristal Bola - Core Dependencies
# Install with: pip install -r requirements.txt
# For optional extras (MongoDB, Parquet), use: pip install -r requirements-all.txt

xai-sdk>=0.1.0
pydantic>=2.0.0
python-dotenv>=1.0.0
